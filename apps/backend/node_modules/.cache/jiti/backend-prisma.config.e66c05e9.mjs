"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.default = void 0;var _dotenv = await jitiImport("dotenv");
var _nodePath = await jitiImport("node:path");
var _config = await jitiImport("prisma/config");

const nodeEnv = process.env.NODE_ENV ?? "development";

// Prisma commands run from apps/backend; runtime environment files are kept at
// the repository root so Docker Compose and the backend share one source.
(0, _dotenv.config)({ path: (0, _nodePath.resolve)("/Users/mdzeeshankhan/smart-healthcare-system/apps/backend", "../../.env." + nodeEnv), override: false });var _default = exports.default =

(0, _config.defineConfig)({
  schema: "prisma/schema.prisma",
  migrations: {
    path: "prisma/migrations",
    seed: "tsx prisma/seed.ts"
  },
  engine: "classic",
  datasource: {
    url: (0, _config.env)("DATABASE_URL")
  }
}); /* v9-dbf98b7dc0d43632 */
